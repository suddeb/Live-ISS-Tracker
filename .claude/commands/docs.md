Add complete JSDoc documentation to the following code.

For each class, method, and exported function add:

/**
 * @description One-line summary.
 *
 * Longer description for complex logic or non-obvious behavior.
 *
 * @param {Type} name - Description and constraints.
 * @returns {Type} What is returned.
 * @throws {ErrorType} When and why this throws.
 *
 * @example
 * // Concise usage example
 */

Output the complete file with all original code preserved — only documentation added.

$ARGUMENTS
